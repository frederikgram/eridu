/* Test comment */


/* Test Various Assignments and definitions */

int age = 1+1;

array bytes = [10011100, 11110000,00110011];

/* Array values initialized at compile-time /
array int values = [1,2,3,4,5];

/* Empty array */
array str names = []

/* Uninitialized Array */
array str fruits;

/* Array of fixed size : no example, don't know how the syntax should be */

callable addr = lambda (x,y,z) = {x+y+z};

define struct DATASET {
    array byte data;
    callable data_reader;
}

str name = "John Doe";
float  height = 1.78 ;
int test_unary = !2;


struct LINKED_LIST_NODE {
    str data;
    struct LINKED_LIST_NODE next;

}

define int LargerThanThree(int a, int b) {
    if (a > 3) {
        return 1;
    }

    return 0;
}

/* Test Various Operators */

int   a = 4;
int   b = 6;
float c = 8.0;

int d = a/b**c;

/* Test some more reserved keywords */
if (d % 12 >= 2) {
  d = 1;
}


define int takes_more_params(int age, str name, float height) {
    /* Does Nothing */
    return 19;
}
