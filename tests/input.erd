/* Test comment */


/* Test Various Assignments and definitions */
int    age = 31;
string name = "John Doe";
float  height = 1.78 ;

define LargerThanThree(int a) {
    
    if (a > 3) { return 1; }
    return 0;
}

/* Test Various Operators */

int   a = 4;
int   b = 6;
float c = 8.0;

int d = a /= b ** c;

/* Test some more reserved keywords */
if (d % 12 >= 2) {
    d = d ^ 1;
}

while (a % c > 0) {
   c++; 
}

for (int i = 0; i < c; i++) {
    b--;
}

define takes_more_params(int age, string name, float height) {
    /* Does Nothing */
    return 1;
}
