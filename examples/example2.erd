/* Madeup code for a fib
number calculator to showcase
the lexer
*/
int fib : int n -> n
    | n <= 1
        == n
    | else
        == add (fib (sub n 1)) (fib (sub n 2))


int res = call fib 5
assert res 5

